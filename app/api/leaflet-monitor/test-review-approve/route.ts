import { NextRequest, NextResponse } from "next/server";
import { requireOperatorApi } from "@/lib/auth/guards";
import { getSupabaseAdmin } from "@/lib/supabase/admin";
import { processLeafletPdf } from "@/lib/leaflet-review/processor";
export const runtime="nodejs"; export const maxDuration=180;
const BUCKET="leaflet-intake", SOURCE_PATH="billa/billa-2026-08-19__a7a0e1fef9b45d3b.pdf";
const eq=(a:unknown,b:number)=>Math.abs(Number(a)-b)<.001;
function headers(req:NextRequest){const h:Record<string,string>={"content-type":"application/json"};const cookie=req.headers.get("cookie"),auth=req.headers.get("authorization");if(cookie)h.cookie=cookie;if(auth)h.authorization=auth;return h;}
async function cleanup(s:any,leafletId:string|null,importId:string|null,path:string,offerId:string|null){if(offerId)await s.from("offers_raw").delete().eq("id",offerId);if(leafletId){const{data:c}=await s.from("leaflet_item_candidates").select("id").eq("leaflet_id",leafletId);const ids=(c??[]).map((x:any)=>x.id).filter(Boolean);if(ids.length)await s.from("leaflet_item_review_audit").delete().in("candidate_id",ids);await s.from("leaflet_notification_outbox").delete().eq("leaflet_id",leafletId);await s.from("leaflet_item_candidates").delete().eq("leaflet_id",leafletId);await s.from("leaflet_page_processing").delete().eq("leaflet_id",leafletId);await s.from("leaflet_documents").delete().eq("id",leafletId);if(importId)await s.from("imports").delete().eq("id",importId);}await s.storage.from(BUCKET).remove([path]);}
export async function GET(req:NextRequest){const gate=await requireOperatorApi();if(!gate.ok)return gate.response;const s=getSupabaseAdmin();if(!s)return NextResponse.json({ok:false},{status:503});const path=`_tests/approve-19-08-2026-25-08-2026-${Date.now()}.pdf`;let leafletId:string|null=null,importId:string|null=null,offerId:string|null=null;try{const{data:source,error}=await s.storage.from(BUCKET).download(SOURCE_PATH);if(error||!source)throw new Error(error?.message||"PDF chybí");const bytes=new Uint8Array(await source.arrayBuffer());const{error:ue}=await s.storage.from(BUCKET).upload(path,bytes,{contentType:"application/pdf",upsert:true});if(ue)throw new Error(ue.message);await processLeafletPdf({supabase:s,bucket:BUCKET,path,retailer:"billa",sourceUrl:"test://approve/billa",page:1,force:true});const{data:doc,error:de}=await s.from("leaflet_documents").select("id,import_id").eq("storage_bucket",BUCKET).eq("storage_path",path).single();if(de||!doc?.id)throw new Error(de?.message||"Dokument nevznikl");leafletId=String(doc.id);importId=typeof doc.import_id==="string"?doc.import_id:null;const{data:rows,error:re}=await s.from("leaflet_item_candidates").select("*").eq("leaflet_id",leafletId).eq("page_no",1);if(re)throw new Error(re.message);const item=(rows??[]).find((x:any)=>eq(x.price_sale,18.9)&&/Tvaroh/i.test(String(x.product_name||"")));if(!item?.id)throw new Error("Tvaroh kandidát nenalezen");const resp=await fetch(new URL("/api/leaflet-review",req.url),{method:"POST",headers:headers(req),body:JSON.stringify({id:item.id,action:"approve"}),cache:"no-store"});const body=await resp.json();if(!resp.ok||!body?.ok)throw new Error(`approve API ${resp.status}: ${body?.error||"unknown"}`);offerId=body.offer_id?String(body.offer_id):null;const{data:saved,error:se}=await s.from("leaflet_item_candidates").select("status,approved_offer_id,reviewed_at").eq("id",item.id).single();if(se)throw new Error(se.message);const{data:audit,error:ae}=await s.from("leaflet_item_review_audit").select("action").eq("candidate_id",item.id).eq("action","approved").order("created_at",{ascending:false}).limit(1).maybeSingle();if(ae)throw new Error(ae.message);const{data:raw,error:oe}=offerId?await s.from("offers_raw").select("id,extracted_name,price_total").eq("id",offerId).single():{data:null,error:null};if(oe)throw new Error(oe.message);const reload=await fetch(new URL(`/api/leaflet-review?leaflet_id=${encodeURIComponent(leafletId)}&page=1`,req.url),{headers:headers(req),cache:"no-store"});const reloadBody=await reload.json();const persisted=(reloadBody.items??[]).find((x:any)=>x.id===item.id);const checks=[{id:"status_approved",value:saved?.status,ok:saved?.status==="approved"},{id:"offer_created",value:raw??null,ok:!!offerId&&raw?.id===offerId&&/Tvaroh/i.test(String(raw?.extracted_name||""))&&eq(raw?.price_total,18.9)},{id:"approved_offer_linked",value:saved?.approved_offer_id??null,ok:String(saved?.approved_offer_id||"")===offerId},{id:"audit_approved",value:audit?.action??null,ok:audit?.action==="approved"},{id:"reload_approved",value:persisted?.status??null,ok:persisted?.status==="approved"&&String(persisted?.approved_offer_id||"")===offerId}];const failures=checks.filter(c=>!c.ok).map(c=>c.id);return NextResponse.json({ok:failures.length===0,point_7a:{pass:failures.length===0,checks,failures}});}catch(error){return NextResponse.json({ok:false,error:error instanceof Error?error.message:String(error)},{status:500});}finally{try{await cleanup(s,leafletId,importId,path,offerId);}catch(error){console.error("approve test cleanup failed",error);}}}
