import { NextRequest,NextResponse } from "next/server";
import { requireOperatorApi } from "@/lib/auth/guards";
export const runtime="nodejs";export const maxDuration=60;
function findPages(value:unknown):Record<string,unknown>[]|null{if(Array.isArray(value)&&value.length&&value.every(x=>x&&typeof x==="object")){const s=value.slice(0,3) as Record<string,unknown>[];if(s.some(x=>"number" in x&&("keyWords" in x||"image" in x||"links" in x)))return value as Record<string,unknown>[];}if(value&&typeof value==="object"){for(const [k,v] of Object.entries(value as Record<string,unknown>)){if(/pages/i.test(k)&&Array.isArray(v)&&v.length){const c=findPages(v);if(c)return c;}}for(const v of Object.values(value as Record<string,unknown>)){const c=findPages(v);if(c)return c;}}return null;}
function summarize(v:unknown,depth=0):unknown{if(depth>3)return typeof v;if(Array.isArray(v))return{type:"array",length:v.length,sample:v.slice(0,3).map(x=>summarize(x,depth+1))};if(v&&typeof v==="object"){const o:Record<string,unknown>={};for(const [k,x] of Object.entries(v as Record<string,unknown>))o[k]=summarize(x,depth+1);return o;}return v;}
export async function GET(req:NextRequest){const g=await requireOperatorApi();if(!g.ok)return g.response;const id=req.nextUrl.searchParams.get("id")||"akcni-letak-od-ctvrtka-20-8-23-8-2026";const u=new URL("https://endpoints.leaflets.schwarz/v4/flyer");u.searchParams.set("flyer_identifier",id);const r=await fetch(u,{headers:{accept:"application/json"},cache:"no-store"});const j=await r.json();const pages=findPages(j);return NextResponse.json({ok:r.ok,status:r.status,page_count:pages?.length??0,page1:pages?.[0]??null,page1_summary:pages?.[0]?summarize(pages[0]):null});}
