import type { OcrWord } from "@/lib/ocr/types";
import { clusterIntoLines, unionWordsBBox } from "@/lib/ocr/price-anchors";
import { parsePriceText } from "@/lib/ocr/price-parse";

export const EXTRACTOR_VERSION = "leaflet-layout-v24";

export type ExtractedCandidate = { candidate_key:string; page_no:number; source_bbox:{x:number;y:number;width:number;height:number}|null; source_text:string|null; product_name:string|null; brand:string|null; variant:string|null; pack_qty:number|null; pack_unit:string|null; pack_unit_qty:number|null; pack_text:string|null; price_sale:number|null; price_standard:number|null; price_loyalty:number|null; price_without_loyalty:number|null; price_per_unit:number|null; price_per_unit_unit:string|null; leaflet_valid_from:string|null; leaflet_valid_to:string|null; item_valid_from:string|null; item_valid_to:string|null; loyalty_required:boolean|null; promo_label:string|null; promo_condition:string|null; minimum_quantity:number|null; field_evidence:Record<string,unknown>; extraction_payload:Record<string,unknown>; extractor_version:string; confidence:number|null; status:"unreviewed"|"quarantine"; review_reason:string|null; };
type Meta={pageNo:number;validFrom:string|null;validTo:string|null}; type Anchor={word:OcrWord;value:number;raw:string;cx:number;cy:number}; type BBox={x0:number;y0:number;x1:number;y1:number}; type Line={words:OcrWord[];text:string;y:number;height:number;bbox:BBox|null}; type ProductBlock={anchor:Anchor;words:OcrWord[];lines:Line[];bounds:BBox;blockIndex:number};

const PACK_RE=/\b(\d+(?:[.,]\d+)?)\s*(g|kg|ml|l|ks|kus|m)\b/i;
const UNIT_PRICE_PREFIX=/(?:1\s*kg|100\s*g|1\s*l|100\s*ml|1\s*m|1\s*ks)\s*=/i;
const GENERIC=/^(?:naše cena|naše|cena|běžná cena|původní cena|cena za|cena při koupi|při koupi|při koupi balení|při koupi od \d+ ks|s klubem|bez klubu|s kartou|bez karty|více druhů|regionální|super pátek|pátek|více víkendových slev uvnitř|slev uvnitř|to dobré začíná u nás|na víkend už|ve čtvrtek|připravte se|do vyprodání zásob\.?|co potěší|v katalogu|akční nabídka|cena za 1 ks|cena za 1 kg)$/i;
const PROMO_ONLY=/^(?:-?\d+\s*%|od \d+\.\s*\d+\. do \d+\.\s*\d+\.|\d+\.\s*\d+\.|\d+\s*(?:ks|bal\.)|cena za \d+\s*(?:g|kg|ml|l|ks))$/i;
const DATE_TEXT=/(?:nabídka\s+platí|platí\s+od|\b(?:od|do)\s+(?:(?:pondělí|pondelí|úterý|utery|středy|stredy|čtvrtka|ctvrtka|čtvrtku|ctvrtku|pátku|patku|soboty|neděle|nedele)\s+)?\d{1,2}\.\s*\d{1,2}\.|\d{1,2}\.\s*\d{1,2}\.?\s*[-–—]\s*\d{1,2}\.\s*\d{1,2}\.|\b20\d{2}\b.*\b(?:od|do)\b)/i;
const MONEY_LIKE=/\b\d{1,3}[,.]\d{2}\b/;
const BAD_NAME_FRAGMENT=/\b(?:běžná cena|původní cena|s klubem|bez klubu|s kartou|bez karty|při koupi|cena za|sleva|nabídka platí|super pátek|více víkendových|do vyprodání|od \d+\.\s*\d+\.|do \d+\.\s*\d+\.|www\.)\b/i;
const NAME_NOISE=/^(?:plech|kombo|druhy|více druhů|balení(?:,.*)?|d\.?o\.?c\.?|co potěší|do vyprodání zásob\.?)$/i;
const PROMO_NAME_NOISE=/^(?:super\s+cena|super\s+akce|akční\s+cena|s\s+lidl\s+plus|lidl\s+plus|ušetřete\s+\d+(?:[.,]\d+)?\s*(?:kč|kc|%)|sleva\s+-?\d+\s*%|jen\s+teď|hit\s+týdne|top\s+nabídka)$/i;

function center(w:OcrWord){return{x:w.x+w.w/2,y:w.y+w.h/2};} function bboxOf(words:OcrWord[],padding=3):BBox|null{return unionWordsBBox(words,padding) as BBox|null;} function publicBox(words:OcrWord[]){const b=bboxOf(words,3);return b?{x:b.x0,y:b.y0,width:b.x1-b.x0,height:b.y1-b.y0}:null;} function lineText(words:OcrWord[]){return words.slice().sort((a,b)=>a.x-b.x).map(w=>w.text.trim()).filter(Boolean).join(" ").replace(/\s+/g," ").trim();} function evidence(raw:string|null,bbox:unknown){return raw?{raw_text:raw,bbox:bbox??null,source:"pdf_text"}:null;} function inside(p:{x:number;y:number},b:BBox,pad=0){return p.x>=b.x0-pad&&p.x<=b.x1+pad&&p.y>=b.y0-pad&&p.y<=b.y1+pad;}
function strictPrice(raw:string):number|null{const s=raw.trim().replace(/\s+/g," ");if(!s||/%/.test(s)||/\b(?:kg|g|ml|l|ks|m)\b/i.test(s))return null;const m=s.match(/^(?:Kč\s*)?(\d{1,3}[,.]\d{2}|\d{1,3},-)(?:\s*Kč)?\/?$/i);if(!m)return null;const n=parsePriceText(m[1]!);return n!=null&&n>0&&n<1000?n:null;}
function makeLine(ws:OcrWord[],medianH:number):Line{const b=bboxOf(ws,0);const hs=ws.map(w=>w.h).sort((a,b)=>a-b);return{words:ws,text:lineText(ws),y:b?(b.y0+b.y1)/2:0,height:hs[Math.floor(hs.length/2)]||medianH,bbox:b};}
function lineObjects(words:OcrWord[],medianH:number):Line[]{const rows=clusterIntoLines(words,Math.max(4,medianH*.52));const out:Line[]=[];const gapLimit=Math.max(12,medianH*1.65);for(const row of rows){const sorted=row.slice().sort((a,b)=>a.x-b.x);let current:OcrWord[]=[];let previous:OcrWord|null=null;for(const word of sorted){if(previous&&word.x-(previous.x+previous.w)>gapLimit){if(current.length)out.push(makeLine(current,medianH));current=[];}current.push(word);previous=word;}if(current.length)out.push(makeLine(current,medianH));}return out.filter(l=>l.text);}
function cleanNameText(text:string):string{return text.replace(/\b\d{1,3}[,.]\d{2}\/?\b/g," ").replace(/-?\d+\s*%/g," ").replace(/\b(?:běžná cena|původní cena|s klubem|bez klubu|s kartou|bez karty|cena při koupi|cena za \d+\s*(?:g|kg|ml|l|ks)|cena za|při koupi(?: balení)?|od \d+ ks)\b/gi," ").replace(/\s+/g," ").trim();}
function isNameText(s:string){const t=s.trim();if(t.length<3||t.length>80||GENERIC.test(t)||PROMO_ONLY.test(t)||NAME_NOISE.test(t)||PROMO_NAME_NOISE.test(t)||DATE_TEXT.test(t)||UNIT_PRICE_PREFIX.test(t))return false;if(PACK_RE.test(t))return false;if(/^[-–—+\d\s.,%/=]+$/.test(t))return false;if(/^(?:\d+(?:[.,]\d+)?\s*)?(?:g|kg|ml|l|ks|m)\b/i.test(t))return false;if(/\b(?:kč|kc)\b/i.test(t)||BAD_NAME_FRAGMENT.test(t))return false;return/[A-Za-zÁ-ž]{3}/.test(t);}
function globalLineForWord(lines:Line[],word:OcrWord){return lines.find(l=>l.words.includes(word))??null;}
function isSecondaryPriceLine(line:Line|null,raw:string){if(!line)return false;const t=line.text;if(UNIT_PRICE_PREFIX.test(t))return true;if(/\b(?:běžná cena|původní cena|bez klubu|s klubem|bez karty|s kartou|cena za 100|cena za 1\s*(?:kg|l|m|ks))\b/i.test(t))return true;const prices=[...t.matchAll(/\b\d{1,3}[,.]\d{2}\b/g)].map(m=>m[0]);return prices.length>1&&!prices.includes(raw.replace(/\/$/,""));}
function lineTouchesX(line:Line,x:number,pad=64){const b=line.bbox;if(!b)return false;return b.x1>=x-pad&&b.x0<=x+pad;}
function boundaryBetween(upper:Anchor,lower:Anchor,lines:Line[]):number{const fallback=(upper.cy+lower.cy)/2;const between=lines.filter(l=>l.y<upper.cy&&l.y>lower.cy&&lineTouchesX(l,upper.cx,70));const secondary=between.filter(l=>upper.cy-l.y<=22&&(/\b\d{1,3}[,.]\d{2}\//.test(l.text)||/\b(?:běžná cena|původní cena|bez klubu|s klubem|bez karty|s kartou)\b/i.test(l.text))).sort((a,b)=>Math.abs(a.y-upper.cy)-Math.abs(b.y-upper.cy))[0];if(!secondary)return fallback;const nextName=between.filter(l=>l.y<secondary.y-2&&isNameText(cleanNameText(l.text))).sort((a,b)=>b.y-a.y)[0];if(!nextName)return fallback;const boundary=(secondary.y+nextName.y)/2;return boundary<upper.cy-3&&boundary>lower.cy+3?boundary:fallback;}
function buildBounds(anchor:Anchor,anchors:Anchor[],page:BBox,lines:Line[]):BBox{let left=Math.max(page.x0,anchor.cx-82),right=Math.min(page.x1,anchor.cx+82),bottom=Math.max(page.y0,anchor.cy-96),top=Math.min(page.y1,anchor.cy+96);for(const other of anchors){if(other===anchor)continue;const dx=other.cx-anchor.cx,dy=other.cy-anchor.cy;if(Math.abs(dy)<=58){if(dx<0)left=Math.max(left,(anchor.cx+other.cx)/2);if(dx>0)right=Math.min(right,(anchor.cx+other.cx)/2);}if(Math.abs(dx)<=80){const upper=anchor.cy>other.cy?anchor:other;const lower=anchor.cy>other.cy?other:anchor;const boundary=boundaryBetween(upper,lower,lines);if(dy<0)bottom=Math.max(bottom,boundary);if(dy>0)top=Math.min(top,boundary);}}return{x0:left,y0:bottom,x1:right,y1:top};}
function crossColumnOwner(word:OcrWord,anchors:Anchor[]):number|null{if(word.w<24)return null;const y=center(word).y;for(let i=0;i<anchors.length;i++)for(let j=i+1;j<anchors.length;j++){const a=anchors[i]!,b=anchors[j]!;if(Math.abs(a.cy-b.cy)>58)continue;const left=a.cx<b.cx?a:b,right=a.cx<b.cx?b:a;const li=a.cx<b.cx?i:j,ri=a.cx<b.cx?j:i;const split=(left.cx+right.cx)/2;if(word.x>=split||word.x+word.w<=split)continue;const leftOverlap=split-word.x,rightOverlap=word.x+word.w-split;if(leftOverlap<word.w*.2||rightOverlap<word.w*.2)continue;const dl=Math.abs(y-left.cy),dr=Math.abs(y-right.cy);if(Math.min(dl,dr)>72)continue;if(Math.abs(dl-dr)<2)return null;return dl<dr?li:ri;}return null;}
function segmentProductBlocks(words:OcrWord[],anchors:Anchor[],medianH:number):ProductBlock[]{const page=bboxOf(words,0);if(!page)return[];const allLines=lineObjects(words,medianH);const bounds=anchors.map(a=>buildBounds(a,anchors,page,allLines));const ownedWords=anchors.map(()=>[] as OcrWord[]);const anchorOwner=new Map<OcrWord,number>();anchors.forEach((a,i)=>anchorOwner.set(a.word,i));for(const word of words){const forced=anchorOwner.get(word);if(forced!=null){ownedWords[forced]!.push(word);continue;}const cross=crossColumnOwner(word,anchors);if(cross!=null){ownedWords[cross]!.push(word);continue;}const wc=center(word);let best=-1,bestScore=Infinity;for(let i=0;i<anchors.length;i++){const a=anchors[i]!;if(!inside(wc,bounds[i]!,2))continue;const dx=Math.abs(wc.x-a.cx),dy=Math.abs(wc.y-a.cy);if(dx>80||dy>96)continue;const score=(dx/72)**2+(dy/78)**2;if(score<bestScore){bestScore=score;best=i;}}if(best>=0)ownedWords[best]!.push(word);}return anchors.map((anchor,blockIndex)=>{const local=lineObjects(ownedWords[blockIndex]??[],medianH).sort((a,b)=>Math.abs(a.y-anchor.cy)-Math.abs(b.y-anchor.cy));const blockLines=local.slice(0,12);const blockWords=[...new Set(blockLines.flatMap(l=>l.words))];if(!blockWords.includes(anchor.word))blockWords.push(anchor.word);return{anchor,words:blockWords,lines:blockLines,bounds:bounds[blockIndex]!,blockIndex};}).filter(b=>b.words.length);}
function sideEvidenceScore(lines:Line[]):number{let score=0;for(const line of lines){const t=line.text;const clean=cleanNameText(t);if(isNameText(clean))score+=2;if(/-?\d+\s*%/.test(t))score+=6;if(UNIT_PRICE_PREFIX.test(t))score+=4;if(PACK_RE.test(t)&&!UNIT_PRICE_PREFIX.test(t))score+=3;if(/\bcena za\b/i.test(t))score+=3;if(/\b\d{1,3}[,.]\d{2}\//.test(t))score+=4;if(/\b(?:běžná cena|původní cena|s klubem|bez klubu|s kartou|bez karty)\b/i.test(t))score+=3;if(/^NAŠE$/i.test(t.trim()))score+=7;if(/^CENA$/i.test(t.trim()))score+=4;}return score;}
function isNearSecondary(line:Line,anchor:Anchor){if(Math.abs(line.y-anchor.cy)>22)return false;return /\b\d{1,3}[,.]\d{2}\//.test(line.text)||/\b(?:běžná cena|původní cena|bez klubu|s klubem|bez karty|s kartou)\b/i.test(line.text);}
function trimToProductCore(lines:Line[],anchor:Anchor):Line[]{const anchorLine=lines.find(l=>l.words.includes(anchor.word))??null;const rest=lines.filter(l=>l!==anchorLine);const above=rest.filter(l=>l.y>anchor.cy+4),below=rest.filter(l=>l.y<anchor.cy-4),near=rest.filter(l=>Math.abs(l.y-anchor.cy)<=4);const aboveScore=sideEvidenceScore(above),belowScore=sideEvidenceScore(below);const chosen=aboveScore>=belowScore?above:below,opposite=aboveScore>=belowScore?below:above;const keep=[...(anchorLine?[anchorLine]:[]),...near,...chosen,...opposite.filter(l=>isNearSecondary(l,anchor))];return[...new Set(keep)].sort((a,b)=>Math.abs(a.y-anchor.cy)-Math.abs(b.y-anchor.cy)).slice(0,12);}
function lineXDistance(line:Line,anchor:Anchor){const b=line.bbox;if(!b)return 999;if(anchor.cx>=b.x0-8&&anchor.cx<=b.x1+8)return 0;return Math.min(Math.abs(anchor.cx-b.x0),Math.abs(anchor.cx-b.x1));}
function chooseNameLines(lines:Line[],anchor:Anchor){const ranked=lines.filter(l=>!l.words.includes(anchor.word)).map(l=>({line:l,clean:cleanNameText(l.text),dy:Math.abs(l.y-anchor.cy),dx:lineXDistance(l,anchor)})).filter(x=>isNameText(x.clean)&&x.dy<=104&&x.dx<=82).map(x=>({...x,score:x.dy*.22+x.dx*.62-Math.min(x.line.height,24)*.45-(x.line.y>anchor.cy?12:0)})).sort((a,b)=>a.score-b.score);if(!ranked.length)return{selected:[] as Line[],ambiguous:false,candidates:[] as string[]};const first=ranked[0]!;const firstSide=Math.sign(first.line.y-anchor.cy)||1;const clustered=ranked.filter(x=>{const side=Math.sign(x.line.y-anchor.cy)||firstSide;return side===firstSide&&Math.abs(x.line.y-first.line.y)<=56&&x.dx<=82&&x.score<=first.score+42;}).slice(0,4);const selected=(clustered.length?clustered:[first]).map(x=>x.line).sort((a,b)=>b.y-a.y);const selectedSet=new Set(selected);const competing=ranked.filter(x=>!selectedSet.has(x.line)&&Math.sign(x.line.y-anchor.cy)!==firstSide&&x.score<=first.score+6);return{selected,ambiguous:competing.length>0,candidates:ranked.map(x=>x.clean)};}
function findExplicitPrice(lines:Line[],rx:RegExp){for(const l of lines){if(!rx.test(l.text))continue;const prices=l.words.map(w=>({raw:w.text,value:strictPrice(w.text),w})).filter(x=>x.value!=null) as Array<{raw:string;value:number;w:OcrWord}>;if(prices.length){const c=prices[prices.length-1]!;return{value:c.value,raw:c.raw,bbox:publicBox([c.w])};}}return null;}
function findStandardPrice(lines:Line[],anchor:Anchor){const explicit=findExplicitPrice(lines,/běžná\s*cena|původní\s*cena/i);if(explicit)return explicit;const slash:Array<{value:number;raw:string;bbox:ReturnType<typeof publicBox>;dy:number;dx:number}>=[];for(const l of lines){if(Math.abs(l.y-anchor.cy)>24)continue;for(const w of l.words){const raw=w.text.trim();if(!/\/$/.test(raw))continue;const value=strictPrice(raw);if(value==null||value<=anchor.value)continue;const p=center(w);const dx=Math.abs(p.x-anchor.cx),dy=Math.abs(p.y-anchor.cy);if(dx>48||dy>24)continue;slash.push({value,raw,bbox:publicBox([w]),dy,dx});}}slash.sort((a,b)=>(a.dy+a.dx*.35)-(b.dy+b.dx*.35));return slash[0]??null;}
function findUnitPrice(lines:Line[]){for(const l of lines){if(!UNIT_PRICE_PREFIX.test(l.text))continue;const m=l.text.match(/((?:1\s*kg|100\s*g|1\s*l|100\s*ml|1\s*m|1\s*ks))\s*=\s*(\d{1,4}[,.]\d{1,2})/i);if(m){const v=parsePriceText(m[2]!);if(v!=null)return{value:v,unit:m[1]!,raw:m[0],bbox:l.bbox};}}return null;}
function findPack(lines:Line[]){const matches:RegExpMatchArray[]=[];for(const l of lines){if(UNIT_PRICE_PREFIX.test(l.text))continue;const m=l.text.match(PACK_RE);if(m)matches.push(m);}const physical=matches.find(m=>!/^(?:ks|kus)$/i.test(m[2]??""));return physical??matches[0]??null;}
function itemValidity(text:string,year:number){const m=text.match(/\bod\s+(?:\p{L}+\s+)?(\d{1,2})\.\s*(\d{1,2})\.?\s+do\s+(?:\p{L}+\s+)?(\d{1,2})\.\s*(\d{1,2})\.?/iu);if(!m)return{from:null,to:null};const p=(n:number)=>String(n).padStart(2,"0");return{from:`${year}-${p(Number(m[2]))}-${p(Number(m[1]))}`,to:`${year}-${p(Number(m[4]))}-${p(Number(m[3]))}`};}

export function extractLeafletCandidates(words:OcrWord[],meta:Meta):ExtractedCandidate[]{if(!words.length)return[];const heights=words.map(w=>w.h).filter(h=>Number.isFinite(h)&&h>0).sort((a,b)=>a-b);const medianH=heights[Math.floor(heights.length/2)]||8;const minMainHeight=Math.max(18,medianH*2.05);const globalLines=lineObjects(words,medianH);const anchors:Anchor[]=[];for(const w of words){const value=strictPrice(w.text);if(value==null||w.h<minMainHeight)continue;const containing=globalLineForWord(globalLines,w);if(isSecondaryPriceLine(containing,w.text.trim()))continue;const p=center(w);anchors.push({word:w,value,raw:w.text.trim(),cx:p.x,cy:p.y});}const dedup=anchors.filter((a,i,all)=>!all.some((b,j)=>j<i&&Math.abs(a.cx-b.cx)<10&&Math.abs(a.cy-b.cy)<10&&Math.abs(a.value-b.value)<.001));const blocks=segmentProductBlocks(words,dedup,medianH);const year=Number((meta.validTo||meta.validFrom||String(new Date().getFullYear())).slice(0,4))||new Date().getFullYear();const out:ExtractedCandidate[]=[];for(const block of blocks){const a=block.anchor;const lines=trimToProductCore(block.lines,a);const coreWords=[...new Set(lines.flatMap(l=>l.words))];if(!coreWords.includes(a.word))coreWords.push(a.word);const nameChoice=chooseNameLines(lines,a);const cleanedNames=nameChoice.selected.map(l=>cleanNameText(l.text)).filter(isNameText);let productName=cleanedNames.join(" ").replace(/\s+/g," ").trim()||null;if(productName&&productName.length>100)productName=null;const nameBox=nameChoice.selected.length?publicBox(nameChoice.selected.flatMap(l=>l.words)):null;const standard=findStandardPrice(lines,a),explicitLoyalty=findExplicitPrice(lines,/klubová\s*cena|s\s+klubem|s\s+kartou/i),explicitWithout=findExplicitPrice(lines,/bez\s+klubu|bez\s+karty/i),unit=findUnitPrice(lines);const source=lines.slice().sort((x,y)=>y.y-x.y).map(l=>l.text).join(" | ");const hasLoyaltyMarker=/\bs\s+klubem\b|\bs\s+kartou\b/i.test(source),hasWithoutMarker=/\bbez\s+klubu\b|\bbez\s+karty\b/i.test(source);const loyalty=explicitLoyalty??(hasLoyaltyMarker?{value:a.value,raw:a.raw,bbox:publicBox([a.word])}:null);const without=explicitWithout??(hasLoyaltyMarker&&standard?standard:(hasWithoutMarker&&standard?standard:null));const pack=findPack(lines),qty=source.match(/při\s+koupi(?:\s+od)?\s+(\d+)\s*ks/i),promo=source.match(/(při\s+koupi[^|]{0,80}|s\s+Klubem[^|]{0,70}|bez\s+Klubu[^|]{0,70})/i),iv=itemValidity(source,year);const loyaltyRequired=hasLoyaltyMarker?true:null;const ambiguousName=!productName||nameChoice.ambiguous||MONEY_LIKE.test(productName)||BAD_NAME_FRAGMENT.test(productName)||DATE_TEXT.test(productName);const hasCore=!ambiguousName&&a.value!=null;const b=block.bounds;out.push({candidate_key:`p${meta.pageNo}-v24-${block.blockIndex}-${Math.round(a.cx)}-${Math.round(a.cy)}`,page_no:meta.pageNo,source_bbox:publicBox(coreWords),source_text:source||null,product_name:hasCore?productName:null,brand:null,variant:null,pack_qty:pack?1:null,pack_unit:pack?pack[2]!.toLowerCase():null,pack_unit_qty:pack?Number(pack[1]!.replace(",",".")):null,pack_text:pack?pack[0]:null,price_sale:a.value,price_standard:standard?.value??null,price_loyalty:loyalty?.value??null,price_without_loyalty:without?.value??null,price_per_unit:unit?.value??null,price_per_unit_unit:unit?.unit??null,leaflet_valid_from:meta.validFrom,leaflet_valid_to:meta.validTo,item_valid_from:iv.from,item_valid_to:iv.to,loyalty_required:loyaltyRequired,promo_label:null,promo_condition:promo?promo[1]!.trim():null,minimum_quantity:qty?Number(qty[1]):null,field_evidence:{product_name:hasCore?evidence(productName,nameBox):null,price_sale:evidence(a.raw,publicBox([a.word])),price_standard:standard?evidence(standard.raw,standard.bbox):null,price_loyalty:loyalty?evidence(loyalty.raw,loyalty.bbox):null,price_without_loyalty:without?evidence(without.raw,without.bbox):null,price_per_unit:unit?evidence(unit.raw,unit.bbox):null,pack_text:pack?evidence(pack[0],publicBox(coreWords)):null},extraction_payload:{segmentation:"word-owner-gap-boundary-v11",block_bounds:b,owned_word_count:coreWords.length,owned_line_count:lines.length,main_price:{raw:a.raw,value:a.value,height:a.word.h,x:a.word.x,y:a.word.y},min_main_height:minMainHeight,lines:lines.map(l=>l.text),name_lines:cleanedNames,name_candidates:nameChoice.candidates,ambiguous_name:nameChoice.ambiguous},extractor_version:EXTRACTOR_VERSION,confidence:hasCore?.64:.1,status:hasCore?"unreviewed":"quarantine",review_reason:hasCore?"Produktový blok používá cenové mezery a cross-column korekci omezenou na stejnou cenovou řadu. Automatické schválení je zakázané.":nameChoice.ambiguous?"V izolovaném produktovém bloku zůstává více konkurenčních názvů. Položka musí do ruční kontroly.":"V izolovaném produktovém bloku není název produktu jednoznačně doložen. Hodnota se nesmí domýšlet."});}return out;}